{
	"extends": ["../../tsconfig.base.json", "@zayne-labs/tsconfig/bundler/dom"],

	"compilerOptions": {
		"paths": {
			"@/*": ["./src/*"]
		}
	},

	"include": ["**/*.ts", "**/*.tsx"],
	"exclude": ["dist", "node_modules"]
}
